// src/app/models/image.model.ts

export interface IimagenVallas {
    id: number; // Asegúrate de incluir el ID u otros campos que necesites
    image_path: string;
    declaracionesanul_id: number;
    image_name: string;
    image_url: string;
  
   
}
